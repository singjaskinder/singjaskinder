import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'social_otp_verify_controller.dart';
class SocialOtpVerify extends StatelessWidget {
const SocialOtpVerify({Key key}) : super(key: key);

            @override
            Widget build(BuildContext context) {
            final controller = Get.put(SocialOtpVerifyController());
            return Scaffold();
           }
        }